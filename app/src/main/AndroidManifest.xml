<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.javacrypto.eudcc">

    <uses-feature android:name="android.hardware.camera.any" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Eudcc"
        android:versionCode="1"
        android:versionName="1.0">
        <activity android:name="de.javacrypto.eudcc.ShowQrcodeDetail" />
        <activity android:name="de.javacrypto.eudcc.AboutActivity" />
        <activity android:name="de.javacrypto.eudcc.ScanQrcode" />
        <activity android:name="de.javacrypto.eudcc.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>